# --- SERVER & DATABASE CONFIG ---

# Default Port for the Express server
PORT=3001

# MongoDB Connection URIs
# Replace <username>, <password>, and <database> with your actual MongoDB credentials.
MONGO_URI="mongodb+srv://<username>:<password>@cluster0.mongodb.net/<database_name>?retryWrites=true&w=majority"
# MONGO_TEST_URI should only be set if you are running tests against a separate, dedicated test database.
# MONGO_TEST_URI="mongodb://localhost:27017/twitchbot_test"


# --- AUTHENTICATION & SECURITY ---

# JWT Secret Key (Crucial for signing/verifying user session tokens)
# Use a strong, random string (e.g., generated by a tool or 'openssl rand -base64 32')
SECRET_KEY=your_very_strong_jwt_secret_key_here


# --- TWITCH BOT TMI.js CONFIG ---

# The Twitch channel the bot will connect to and listen/respond in
TWITCH_CHANNEL_NAME=your_twitch_channel_name

# The bot's own Twitch username (needs to be registered as a separate account)
TWITCH_BOT_USERNAME=your_bot_username

# OAuth Token for the bot account (Obtained from a site like twitchapps.com/tmi)
# It should look like "oauth:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
TWITCH_BOT_OAUTH_TOKEN=your_bot_oauth_token_here


# --- TWITCH OAuth (Passport) CONFIG ---

# Used for authenticating users (dashboard access, API calls) via Passport.js
TWITCH_CLIENT_ID=your_twitch_developer_client_id
TWITCH_CLIENT_SECRET=your_twitch_developer_client_secret

# This should match the Redirect URI configured in your Twitch Developer App
# Change the hostname/port if deploying to production
TWITCH_CALLBACK_URL="http://localhost:3001/api/twitch/auth/callback"


# --- DISCORD OAuth (Passport) CONFIG ---
# Required for connecting users to Discord, mentioned in discordRoutes.js (not provided)

DISCORD_CLIENT_ID=your_discord_developer_client_id
DISCORD_CLIENT_SECRET=your_discord_developer_client_secret
# DISCORD_CALLBACK_URL="http://localhost:3001/api/discord/auth/callback"
